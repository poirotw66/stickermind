# Copy to .env.local and fill in. Do not commit .env.local (it is gitignored).

# Required for the API server (npm run server). Never put this in the frontend.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: only needed when building the frontend for a host that is not same-origin as the API.
# Example: VITE_API_BASE_URL=https://your-api.example.com
# VITE_API_BASE_URL=
